[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "spendflow"
version = "0.1.0"
description = "A procurement data-quality and savings-opportunity assistant."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]